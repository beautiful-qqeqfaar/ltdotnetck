<Window x:Class="thick.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:thick"
        mc:Ignorable="d"
        Title="Window1" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="dt" Grid.Row="7" Grid.ColumnSpan="2"
          AutoGenerateColumns="False"
          ColumnHeaderStyle="{StaticResource 5}">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ma lop" Binding="{Binding MaHS}"/>
                <DataGridTextColumn Header="Ten lop" Binding="{Binding Lop}"/>
                <DataGridTextColumn Header="So hs nu" Binding="{Binding So}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
